export interface BookingProcess {
  selectedBookingOption: number;
  locationName: string;
  locationId: string;
  regionId: string;
  selectedDateOption: number;
  startDate: Date;
  endDate: Date;
}
