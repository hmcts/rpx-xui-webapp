import { Injectable } from '@angular/core';
import { Observable } from 'rxjs';

@Injectable()
export class NocService {

  constructor(
  ) { }

  getQuestions(payload): Observable<any> {

    return null;
  }

  submitAnswers(payload): Observable<any> {

    return null;
  }
}
