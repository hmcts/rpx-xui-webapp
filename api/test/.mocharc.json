{
    "exit": true,
    "bail": true,
    "slow": 1000,
    "recursive": true,
    "extension": ["ts"],
    "spec": ["**/*.spec.ts"],
    "exclude": ["**/pact/**"],
    "require": ["ts-node/register", "source-map-support/register"]
}
